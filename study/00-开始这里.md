# 第 1 课：从零开始读 DSH

> **本课是社区导读，不是官方教程。** 它解释固定提交中的源码和公开资料，帮助社区建立阅读路线；其中的判断、建议和研究问题不代表 DeepSeek AI 的承诺。

这是一份给第一次读大型程序的人准备的路线图。你不需要先记住所有英文名；先把 DSH 想成一个“能让模型工作、调用工具、保存过程、让插件替换零件”的程序。

如果你从 `START-HERE` 进入，这就是现在要读的第一篇正文；读完后不要随机打开索引，先按本页的推荐顺序走一小段。

<div class="dsh-lesson-contract" aria-label="第一课课程卡">
  <div class="dsh-lesson-contract-item"><span>本页目标</span><strong>建立最小心智模型</strong><small>先分清插件、服务、事件、Profile、Bundle 和 Turn。</small></div>
  <div class="dsh-lesson-contract-item"><span>预计时间</span><strong>5–10 分钟</strong><small>读懂六个词就可以先停，不需要顺读整个仓库。</small></div>
  <div class="dsh-lesson-contract-item"><span>你会留下</span><strong>一条阅读记录</strong><small>用途、证据、边界和下一跳各写一句。</small></div>
  <div class="dsh-lesson-contract-item"><span>下一步</span><strong>打开 01-仓库地图</strong><small>把刚才的概念落到目录、入口和测试线索。</small></div>
</div>

## 先读本教材自己的总论

下面这组判断是本学习仓库对 DSH、Harness 和软件 Agent 的研究视角，不是 DeepSeek 官方声明。固定版本源码、测试和真实实验分别能证明不同事情，后文会把它们拆开。

<div class="dsh-overview-summary" aria-label="教材的核心判断">
  <span class="dsh-overview-kicker">本教材的核心判断</span>
  <strong>组件变多，不等于效果已经变好。</strong>
  <p>能分步完成任务的程序（Agent），基本工作方式仍然是“观察 → 决定 → 动手 → 看结果 → 更新情况 → 再观察”。</p>
  <small>运行框架（Harness）、可渐进加载的说明（Skill）、浏览器操作、截图、记忆、资料检索（RAG）、工具筛选和自动修改，主要是在改变这个循环能看到什么、能做什么以及怎样判断结果。</small>
</div>

循环变复杂，也不等于 Agent 变聪明。读 DSH 时，先问“哪一层变了、引入了什么失败、有什么证据”，再问“要不要继续加组件”。

### 先把常见术语翻译成人话

第一次读源码时，不要因为英文词多就停下来背词。先用下面的意思理解；等真的要查代码时，再用括号里的英文搜索。

| 文中写法 | 先按这个意思理解 |
| --- | --- |
| Agent / 智能体 | 能根据结果继续决定下一步的程序 |
| Agent loop / 工作循环 | 反复“看情况 → 做一步 → 看结果” |
| Tool / 工具 | 模型可以请求宿主执行的一项动作 |
| schema / 参数说明 | 告诉模型这个动作需要哪些参数、参数长什么样 |
| Profile / 运行清单 | 这次启动准备打开哪些功能 |
| Bundle / 功能组合 | 一起安装、一起装配的一组插件 |
| Provider / 模型服务方 | 真正提供模型请求服务的一方 |
| Hook / 外部钩子 | 外部程序和宿主约定的一次输入输出 |
| Patch、Fork、注入 | 分别是改配置或源码、另维护一份源码、运行中改写宿主 |

后文仍会使用这些英文词，因为它们是源码中的真实名称；但每次遇到时，先回到上表，不需要先把术语背完。

### 1. Agent 核心是一个工作循环（Agent loop），但不是只有一个循环

可以先把一次任务画成最小循环：

```text
观察上下文与外部状态
  -> 决定下一步
  -> 调用模型或工具
  -> 接收结果与副作用
  -> 更新状态
  -> 再次观察
```

真正影响结果的，不只是“循环”这个词，还包括它看到了什么、能选择哪些动作、模型怎样决定、工具和环境是否可靠，以及失败后怎样重试、退回或换办法。

记忆还要回答写入、召回、过期和冲突问题；权限还要回答谁能调用、谁来审批和越权如何拒绝；评估还要回答一次改动是否真的改善了任务，而不是只让架构图更复杂。

可以把这个教材抽象写成：

```text
Agent
  = loop
  + 模型与提示词
  + 上下文与记忆
  + 工具与运行环境
  + 权限与失败恢复
  + 评估标准
```

“所有设计底层都是 loop”是一个有用的抽象，但不能推出所有 Agent 架构能力相同。Planner、Executor、路由器、状态机和多 Agent，通常是多个循环、嵌套循环或循环与状态机的组合。

### 2. “纯软 Agent 到头了”要改成更准确的话

“纯软 Agent 基本到头”可以表达一种工程直觉，但不适合作为绝对结论。更准确的说法是：**朴素的单循环式 Agent 已经遇到明显的边际收益和验证瓶颈。**

这不等于后面没有研究方向。分层 Agent、事件驱动流程、专用模型、外部执行环境、工具路由、权限控制和更好的记忆系统，仍然可能改善可靠性、延迟、成本或可维护性。

它们依然需要回答同一个问题：改变了循环的哪一层，改善了哪个指标，付出了什么新成本，失败时能不能恢复。Agent 可以继续进化，底层仍然可以是循环，这两句话并不矛盾。

### 3. 没有大量数据时，仍然可以验证一部分

“没有大量数据就几乎不能验证”也需要拆成两层。没有大规模用户数据时，仍然可以验证机制、回归和固定任务上的差异：

| 没有大规模真实数据仍可验证 | 需要更大、更分层的数据才能判断 |
| --- | --- |
| 组件是否按设计工作 | 是否对复杂真实用户普遍有效 |
| 工具可见集合是否真的变化 | 是否对不同模型和 Provider 都有效 |
| RAG 是否召回预期证据 | 结果能否推广到不同任务分布 |
| 工具路由是否找得到、选得对 | 收益是否稳定且具有足够统计把握 |
| 固定任务集上的 A/B 和回归 | 一次改动对真实用户的长期影响 |

小型固定基准、离线 replay、确定性夹具和配对 A/B 可以告诉我们“这个机制是否工作”或“这里是否发生回归”。它们不能自动告诉我们“所有用户都会受益”。

### 4. 自然用户日志不能直接证明因果收益

用户数据通常同时混入任务难度、用户熟练度、模型版本、Provider、工具数量、工具顺序、Prompt、上下文长度、网络延迟和失败重试策略。

如果只看到“改完之后成功了”，还不知道成功来自 Harness 改动、模型变化、任务变化、用户熟练度，还是只记录了成功样本。要回答“哪次改动造成了多少提升”，这已经是因果比较，不是普通日志统计。

最低限度要固定运行框架（Harness）、模型、模型服务方（Provider）、任务分组、工具集合和主要配置，再用两组尽量公平的 A/B 对照、主要指标、不能变差的指标、保留题目（holdout 任务）、失败样本、成本记录和可回退版本。

只有相关日志时，应写“观察到关联”；有受控实验时，才可以写“在指定任务、模型和时间范围内观察到差异”。不要把一次成功案例写成普遍规律。

### 5. RAG、工具延迟加载和自修改仍是开放问题

资料检索（RAG）不能只问“有没有找回几段文字”，还要看前几条里有多少正确资料、证据是否覆盖问题、排序是否合理、噪声和截断是否过多、没有答案时会不会拒答，以及最终正确性、延迟和成本。找回结果不为空，不等于回答有证据。

工具列表也没有对所有任务都最好的固定答案。全部显示、按运行清单（Profile）显示、按任务范围显示、先给一个小目录再按需显示参数说明（schema）、自动筛选和失败后扩大搜索，都要比较“能不能找到”“选得对不对”、上下文成本、第一次调用等待时间、任务成功率和恢复率。

自动修改提示词（Prompt）、工具配置、模型、说明文档（Skill）、记忆或压缩办法，首先只是“找一个可能更好的配置”。自动产生候选改动，不等于已经证明改动有益；要有版本、目标、测试题、不能变差的指标、人工批准和回退办法。

本教材会把这些问题落回 DSH 的具体层次：[工具可见性与非侵入扩展](22-工具可见性与非侵入扩展.md)讨论候选工具怎样进入循环，[工具可见集合观测与性能实验](23-工具可见集合观测与性能实验.md)讨论怎样测量，[后续研究路线](26-后续研究路线.md)讨论怎样把差异变成可信证据。

### 6. DSH 可以借鉴 Linux，但还不能叫“新时代 Linux”

这个比喻有启发，但不能当成已经实现的事实。Linux 不是一个插件框架，而是内核、发行版、软件包、工具和社区规则一起形成的基础系统；这些规则也是多年维护后才稳定下来的。

Linux 里已经有一些和 DSH 类似的解决思路，但它们分散在不同层次：程序有相对稳定的系统调用，用户有文件和进程权限，软件可以安装、升级、卸载，部分系统还支持快照和回滚。

| Linux 的经验 | 用人话翻译成 DSH 的问题 |
| --- | --- |
| 稳定的系统调用和软件接口 | 插件应该按照什么规则接入，哪些规则会长期不变？ |
| 命令只有在需要时才运行 | 工具可以安装很多，但不要每轮都把所有工具说明交给模型。 |
| 内核执行用户和进程权限 | 权限应由宿主真正检查，不能只让插件自己声明“我安全”。 |
| 软件包、版本和系统快照 | 插件要能说明版本，出错时要能禁用、卸载或退回旧版本。 |
| 内核测试、发行版测试和性能测试 | 改动要用固定任务重复测试，不能只看一次成功回答。 |

DSH 还多一个 Linux 没有完全相同的问题：工具的名称、参数和说明要进入模型上下文。系统里“已经安装”不等于模型“这一轮看得见”，模型“看得见”也不等于当前任务“有权使用”。

因此，最值得记住的一句话是：**工具可以很多，但每轮只给模型真正需要的那一小部分；能看见、能调用和运行可靠，是三件不同的事。**

Linux 也没有一本能代表全部 Linux 的统一白皮书。内核有开发文档，发行版有自己的手册，社区再补充教程、案例和书籍。DSH 未来也需要这种分工，但官方必须先把公开规则和不保证的部分说清楚。

### 读 DSH 源码时的四个问题

以后看到一个新插件、新工具或新 Harness 组件，先按下面顺序问：

1. 它改变了循环的观测、上下文、动作、状态、策略还是评估？
2. 它解决了什么问题，又引入了什么新的失败、延迟、成本或权限风险？
3. 证据属于源码事实、单元测试、本地宿主实验、真实 Provider 还是模型任务结果？
4. 还有什么没有验证，怎样回滚，谁对这部分负责？

这四个问题比“这个项目是不是自进化”“工具是不是越多越强”更适合带着读源码。后面的每一篇课文都会重复使用这套判断方法。

## 先记住六个词

- **插件**：可以插入或拔出的功能模块。DSH 里模型适配器、工具注册表、Session、Agent Loop 和 Web 组件都可以作为插件装配；“模型”本身不等于插件。
- **服务**：插件放进共享上下文后，其他插件可以取得的能力，例如 `ctx.sessions` 或 `ctx.tools`。
- **事件**：带有类型和派发模式的运行时扩展点。有的事件只允许观察，有的可以作出决策或包裹流程；发送者不必知道所有接收者，因此新功能可以旁路接入。
- **Profile**：一次运行使用哪几层功能的名单，像一张装配清单。
- **Bundle**：一层可安装的功能组合，通常通过补丁把很多插件放进树里。
- **Turn 与 Step**：通常一次用户输入会触发一个 Turn。一个 Turn 可以包含 0 个或多个 Step；每个 Step 是一次模型请求，以及这次请求触发的工具调用。若首次输入被拒绝或被改写为空，也可能记录一个没有 Step 的 Turn。

> 关键提醒：**“一切皆插件”描述的是能力如何装配，不是“所有插件都必须把工具发给模型”。** 工具还要经过作用域、呈现方式和执行策略；后面读到“工具可见性与非侵入扩展”时，再把“已注册、模型可见、执行允许”分开。

## 先看一个贯穿示例

假设你想写一个“只记录工具结果、不改变工具行为”的小插件。第一次不要从 Loader 或模型请求开始，先把问题压缩成四句话：

```text
入口：公开的 tools/result 事件。
动作：收到最终结果后，只生成一段限长文本预览。
已经能证明：源码和单元测试显示它订阅了事件，并且不会改写输入结果。
还没有证明：真实 DSH Profile 已加载它，或模型因此收到了更少 token。
```

这四句话就是本教材反复使用的阅读方法：先找公开入口，再找实际动作，然后把源码/测试证据和仍缺少的运行证据分开。后面的[最小插件示例](../study-examples/minimal-observer-plugin/README.zh.md)会把这四句话变成可以运行的 Node 测试；它仍然不会自动变成真实 DSH 安装证明。

## 这一页读完算什么

- [ ] 我能用自己的话解释插件、服务、事件、Profile、Bundle 和 Turn。
- [ ] 我能画出“输入 → Prompt → 模型 → 工具 → Session”的最小链路。
- [ ] 我能用“已证明／还没有证明”描述一个源码结论，而不是只说“它应该能用”。

如果三项中有一项说不清，停在本页或回到[从这里开始](../START-HERE.md)；不需要为了证明自己已经入门而打开逐文件索引。

## 推荐阅读顺序

1. 读[仓库地图](01-仓库地图.md)，知道文件在哪一层。
2. 读[Cordis 与插件树](02-Cordis与插件树.md)，理解 `Context`、服务、事件和可撤销效果。
3. 读[核心文件精读](03-核心文件精读.md)，只先读列出来的主链路文件。
4. 读[Agent 与 Turn 流程](04-Agent与Turn流程.md)，把一次请求按时间顺序串起来。
5. 读[Session 日志与恢复](05-Session日志与恢复.md)，理解为什么事件要持久化。
6. 读[LLM 与工具执行](06-LLM与工具执行.md)，理解模型输出怎样变成工具调用。
7. 读[Host、Client、示例、测试与发布](07-HostClient示例测试发布.md)，把运行宿主、示例、测试层次和验证边界接起来。
8. 最后打开[逐文件索引](08-逐文件索引怎么读.md)，按兴趣跳到 2,756 个文件中的任何一个。
9. 如果你想扩展 DSH，先读[社区生态与扩展边界](10-社区生态与扩展边界.md)，分清公开插件、配置 patch、源码 fork 和运行时注入。
10. 再读[如何写一个合规插件](11-如何写一个合规插件.md)，从观察事件开始，逐步做到工具注册、Bundle 安装、卸载和测试。
11. 准备安装社区项目或建立自己的目录时，再读[GitHub 生态检索与插件实战核验](12-GitHub生态检索与插件实战核验.md)，学习怎样确认每个候选仓库的身份、装配格式、权限、版本和测试证据。
12. 如果你担心“万物皆插件”带来过多工具，先读[工具可见性与非侵入扩展](22-工具可见性与非侵入扩展.md)，分清注册、模型可见和执行允许。
13. 想写工具插件时，读[官方工具插件完整契约](13-官方工具插件完整契约.md)，先理解工具流水线和可见性，再写最小实现。
14. 想接入 Claude Code、Codex 或外部 shell hook 时，读[官方 Hook Bridge 与兼容层](14-官方HookBridge与兼容层.md)，先区分协议桥接和普通插件。
15. 想发布可安装组合时，读[Bundle、Profile、Loader 与发布安装](15-BundleProfileLoader与发布安装.md)，理解 patch、Profile、解析和启动生命周期。
16. 想第一次照着做时，打开[学习工作簿与首个实验](16-学习工作簿与首个实验.md)，按练习顺序留下自己的阅读和实验记录。
17. 想确认这份材料到底覆盖了什么，读[完成度审计与证据矩阵](17-完成度审计与证据矩阵.md)，把“已覆盖”和“未验证”分开。
18. 想切换到新的上游版本时，读[维护、更新与版本迁移](18-维护更新与版本迁移.md)，不要直接在旧索引上覆盖新源码。
19. 最后读[插件测试、卸载与版本证据](19-插件测试卸载与版本证据.md)，把“能运行”升级为可复核的安装、失败、取消和卸载证据。
20. 如果你不知道每一步该怎么做，打开[学习仓库实际使用手册](20-学习仓库实际使用手册.md)，按目标选择路线并记录自己的证据。
21. 想把“工具太多”变成可测问题时，读[工具可见集合观测与性能实验](23-工具可见集合观测与性能实验.md)，先做离线快照，再决定是否需要真实模型。
22. 想检查自动索引有没有把模板套错地方时，读[高风险索引人工抽查](24-高风险索引人工抽查.md)，学习怎样从卡片回到固定源码和测试。
23. 想把课程问题交给 Debug 做一次可审计的脱敏证据检查时，读[研究与 Debug 协作](29-研究与-Debug-协作.md)；那一页内嵌了离线桥接工作台。
24. 想亲手比较 Code Mode 的 deny/allow、policy lane、主体执行次数和并发屏障时，读[确定性可视化实验协议与 Code Mode 权限管线](33-确定性可视化实验协议与Code-Mode权限管线.md)；那一页内嵌了证据实验，可以逐帧检查。

## 一个最小的心智模型

可以把一次请求画成：

```text
用户输入
  -> Agent 收到输入
  -> Session 记录输入
  -> Prompt 组装上下文和工具说明
  -> LLM 产生流式回答
  -> Agent 判断是否要调用工具
  -> Tool 执行并返回结果
  -> Session 记录结果
  -> 需要时进入下一步，否则结束 Turn
```

这里的箭头不是某一个函数直接调用所有东西，而是许多插件通过服务和事件连接起来。读源码时要问两件事：这条信息在哪里产生，最后由谁消费；这项能力是稳定接口，还是某个可替换提供方。

读扩展生态时还要问第三件事：我使用的是上游公开接口，还是自己 fork 中新增的内部接口？前者可以写成第三方插件；后者应写成 patched fork 的私有扩展点，不能只因为它能运行就称为官方插件。

## 怎样读一个文件

先读文件所在包的 README，再读 `index.ts` 入口，然后看类型和事件，接着看实现，最后看同包测试。遇到一个名字时，不要只看它“做了什么”，还要看它为什么没有放进另一个文件：通常答案是边界、替换、测试或生命周期。

## 读到不懂时怎么办

不要从一个 2,000 行文件的第一行硬读到最后。先从[核心精读](03-核心文件精读.md)中的文件开始，再顺着“直接协作者”和测试链接跳转。索引中标为“自动索引”的条目只提供方向；涉及行为时必须回到固定版本源码核对。
